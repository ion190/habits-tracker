# TODO

- [ ] Create “Today writing note” block on `src/pages/JournalPage.tsx` and render it before other sections when `period=daily` + current `dateKey`.
- [ ] Improve desktop styling for the journal page editor area (spacing/width/typography) while preserving current mobile behavior.
- [ ] Verify textarea newline rendering and general layout at desktop (~1024px+) and mobile (<800px).
- [ ] Run dev server and manually test `/journal?period=daily&key=<today>` and non-today keys.

